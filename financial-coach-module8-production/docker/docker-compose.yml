services:
  postgres:
    image: postgres:16
  redis:
    image: redis:7
  backend:
    build:
      context: ../backend
      dockerfile: ../docker/Dockerfile.backend
  frontend:
    build:
      context: ../frontend
      dockerfile: ../docker/Dockerfile.frontend
